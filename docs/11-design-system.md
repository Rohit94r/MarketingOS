# Design System

The interface uses restrained surfaces, black text, blue accents, and glass-like panels. Components are intentionally simple so the product feels like a working tool rather than a static brochure.
